class StraightLine {
  constructor(vertex_positions) {
    this.vertex_positions = vertex_positions
  }

  getVertices() {

    for (let i = 1; i < this.vertex_positions - 1; i++) {
      let prev = this.vertex_positions[i-1]
      let cur = this.vertex_positions[i]
      let next = this.vertex_positions[i+1]

      let l1 =
    }
    // return this.vertices
  }
}
